database:
   type: mysql
   host: 185.35.187.91
   username: test
   password: test123
   database: dev_emf ## test: dm_test


debug:
  enabled: "yes" #false will disable all below
  #types: ["callback_match"]
  timespent: "yes"
  category: db, action, template
  levels: error, warning, info
  ip: 127.0.0.1

##sites define all the sites
sites:
  demosite:
    routes:
      - host: "localhost:8092" ##separated by ",", support regular expression like{sub:[0-9]+}
        path: "demosite,test" ##separated by ","
      - host: "localhost:8092"
    root: 55
    default: 55
    resource_prefix: "static" #eg. static/assets
    template_folder:
      - "demosite"  #first template folder will be used for base template(layout)
      - "test" # todo: idea to support package like "<packagename>, test"
      #todo: support path from package. eg. dm/core/admin/templates/admin. question: then how to extend admin?
  #site test1
  test1:
     routes:
       - host: "localhost:8092"
         path: "test1"
     root: 55
     default: 55
     resource_prefix: "static"
     template_folder:
       - "demosite"
       - "test"
  #site test1
  admindesign:
     routes:
       - host: "localhost:8092"
         path: "admindesign"
     root: 55
     default: 55
     resource_prefix: "static"
     template_folder:
       - "admindesign"
       - "test"

controllers:
  user/view:
    site:
        - demosite

template:
    folder: ["demosite"]

# image:
#     alias: {thumbnail:{width: 30}, large:{width: 800}}
#     generated_when_upload: "thumbnail, large"
